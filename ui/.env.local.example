# Frontend Environment Variables

# Backend API URL
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Authentication token (will be set dynamically)
# NEXT_PUBLIC_AUTH_TOKEN=your-demo-token-here

# Optional: API polling interval (ms)
NEXT_PUBLIC_POLLING_INTERVAL=5000
